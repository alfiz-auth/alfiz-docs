# Source: https://alfiz-web.vercel.app/pricing

Pricing

# Hosted administration is convenience. Federation is capability.

Stated plainly, because the split is architectural: the hosted tier withholds no workflow the headless kit can build. What you pay for there is assembly, polish, hosting and seats. Only federation is machinery that genuinely requires central infrastructure.

Annual billingTwo months free on hosted seats and federation.

Open source

### Client + Application

FreeMIT, forever

Complete for one application. The full feature set, no external dependency beyond your own database.

[npm i @alfiz-auth/core](https://github.com/alfiz-auth)

Permission grammar, catalog, typed keys

can / canAny / can.fresh, four enforcement points

Static verification and catalog lints

Scoped grants, revokes, expiry, provenance

Groups, nested groups, roles, reporting hierarchy

Access requests and management-layer approvals

Headless admin kit + unstyled reference assembly

Clerk adapter; provider-agnostic interface

Hosted administration

### Alfiz Cloud

$20per admin seat / month

The day a non-engineer needs to administer access. Your Application stays the org root and the sole writer.

Talk to us about seats

Everything in the OSS layers

Hosted, styled, maintained dashboard

Admin seats for non-engineer administrators

Hosted approvals inbox, rendered by relay

Opt-in audit retention and export

Replaceable by construction — self-assemble the same surface from the kit

Capability

### Federation

From $900per month, plus per application

When the second application arrives. Central catalog registry, cross-application composition, the org root.

Talk to us

Everything in hosted administration

Catalog registry: namespace ownership, versioning, tombstones

Cross-namespace role composition

Cross-application request aggregation

Provision-and-reconcile integrations, drift reports

Service-principal credential minting and rotation

Cross-application audit aggregation

Audited promotion, demotion, and merges

What we meter

## Usage observed is work performed.

An idle link accrues nothing. Current counts per dimension are provider data readable over the same contract the dashboard uses, so a self-built admin page renders them identically.

Never metered

Runtime checks. They never touch us in any topology — per-check pricing is impossible by construction, and that is a guarantee, not a policy.

Admin seats$24 / seat / month

Linked application$0 · included

Federated application$150 / app / month

Provisioned connector$90 / connector / month

Retained audit volume$0.60 / 10k events

Catalog publishesGuardrail dimension

Org-root read-model syncGuardrail dimension